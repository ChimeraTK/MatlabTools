%%
%

ver = mtca4u.version();

requ_ver = '@DeviceAccess_Matlab_FULL_LIBRARY_VERSION@';

assert(strcmp(ver,requ_ver) ~= 0, ['Wrong version. (Returned ''', ver ,''' but ''', requ_ver ,''' expected)']);
